# Recon Output Artefacts

This directory stores reconnaissance results generated by `run_recon.py`. The
files are intentionally tracked by Git so that sample runs can be shared and used
as fixtures for improving the automation. Remove or replace the contents before
committing if they include sensitive information.
